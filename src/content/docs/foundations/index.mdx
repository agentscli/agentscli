---
title: Overview
description: A Rosetta stone for AI coding CLIs — concepts mapped across Claude Code, Codex, and OpenCode.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import PrimitivePicker from '../../../components/interactive/PrimitivePicker.tsx';

These guides compare three AI coding CLIs along **concept lines**, not tool lines. Pick a concept, see how it works in each tool side by side. Tab selections sync across pages — pick "Claude Code" once and every chapter opens to that tab.

If you're new to how AI coding agents actually work — the loop, the context window, why sessions are independent — start with **[How agents work](/foundations/how-agents-work/)**. It's the shared substrate every chapter below assumes.

The three tools in scope:

- **[Claude Code](https://code.claude.com/docs)** — Anthropic
- **[Codex CLI](https://developers.openai.com/codex/cli/)** — OpenAI
- **[OpenCode](https://opencode.ai/docs/)** — SST (open source)

## Find the right primitive

Twelve chapters is a lot of doors. If you already know the problem you're trying to solve, start from the problem:

<PrimitivePicker client:visible />

## The translation table

Every row links to the chapter for that concept. Cells show the tool's term for the same idea — or `—` if it has no equivalent.

| Concept | Claude Code | Codex | OpenCode |
|---|---|---|---|
| [Rules](/foundations/rules/) | `CLAUDE.md`, `.claude/rules/` | `AGENTS.md` | `AGENTS.md` |
| [Subagents](/foundations/subagents/) | Subagents, Agent teams (exp.) | Subagents | `general` / `explore` / `scout` + custom |
| [Model selection](/foundations/model-selection/) | `/model` (Anthropic only) | `/model`, `--model` (OpenAI) | Per-agent, multi-provider |
| [Slash commands](/foundations/slash-commands/) | Bundled + skill-invocable | Built-in + `~/.codex/prompts/` | `commands/` |
| [Skills](/foundations/skills/) | Skills (`SKILL.md`) | Skills (`SKILL.md`) | `skills/` |
| [MCP servers](/foundations/mcp-servers/) | `.mcp.json`, `/mcp` | `config.toml`, can also *be* an MCP server | Configurable |
| [Hooks](/foundations/hooks/) | Lifecycle hooks in `settings.json` | Lifecycle scripts | Plugin hooks (JS/TS) |
| [Permissions & sandboxing](/foundations/permissions/) | Allow/ask/deny + managed policy | Sandbox tiers + approval policy | Per-tool, per-agent |
| [Plan mode](/foundations/plan-mode/) | Shift-Tab toggle | Emergent (`untrusted` + `read-only`) | `plan` primary agent |
| [Configuration](/foundations/configuration/) | `settings.json` (layered) | `config.toml` + named profiles | `opencode.json` |
| [Headless / CI](/foundations/headless/) | `claude -p` | `codex exec` | `opencode run` |
| [Plugins & marketplaces](/foundations/plugins/) | Plugins + marketplaces | — | JS/TS plugins |

## How to read these guides

Each chapter has the same shape:

1. **What the concept is** — tool-agnostic explanation, one or two paragraphs.
2. **How it works** — a tabbed block with one tab per tool, showing real paths, syntax, and gotchas.
3. **Comparison** — a small table summarising the differences.
4. **Name collisions** — where the same word means different things across tools, and how to keep it straight.

## A note on name collisions

The same word can mean different things across these tools. The chapters call these out individually, but a few are worth flagging up front:

- **"Agents"** — OpenCode has *primary agents* (`build`, `plan`); Claude Code has *Agent teams* (independent peer sessions, experimental); all three have *subagents*. Always qualify which kind.
- **"Plan mode"** — a Claude Code UI toggle, an OpenCode *primary agent*, and a Codex *sandbox + approval combo*. Same posture, three implementations.
- **"Hooks"** — first-class lifecycle handlers in Claude Code and Codex; in OpenCode they're registered from inside JS/TS plugins.
- **`AGENTS.md`** is a real shared standard. Codex and OpenCode read it natively. Claude Code reads `CLAUDE.md`, but you can import `@AGENTS.md` for one source of truth across all three tools.
