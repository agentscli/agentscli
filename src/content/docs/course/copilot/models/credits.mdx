---
title: Spending credits where it pays
description: "Heavier model and feature choices eat more of Copilot's usage budget - and on a company plan, what you can spend was decided above your account."
---

import ModelEconomy from '../../../../../components/interactive/ModelEconomy.tsx';

The model-picker lesson kept gesturing at a cost. For current plans, heavier usage draws on a budget of [**GitHub AI Credits**](https://docs.github.com/en/copilot/concepts/billing/organizations-and-enterprises) - individual plans carry their own allotments and usage views, covered with the tiers on [the plans page](https://github.com/features/copilot/plans). Some existing individual subscriptions may still use the older [**premium-request**](https://docs.github.com/en/copilot/reference/copilot-billing/request-based-billing-legacy/copilot-requests) system, but those are separate billing models. In either case the principle is simple: **the more expensive the model or feature, the more usage budget it can consume.** The exact cost depends on the plan, model, surface, and current GitHub pricing.

You do not need to memorize a fixed per-request price - availability and rates change. The useful mental model is qualitative: use the stronger model or feature where it can change the outcome, and check the usage view for your plan. On a shared-library refactor, extra reasoning may be worth the budget; on routine CRUD in your own service, a lighter or automatically selected model may be enough.

The widget keeps generic dials - model capability and thinking effort - where Copilot's equivalents are choosing a stronger model and asking for deeper reasoning on the same context; the ledger underneath is the same one. Run a day of it and watch where a pinned premium setting leaks - and note the asymmetry the day exposes: skimping on a hard problem is the costly mistake, because failed attempts plus the escalation you run anyway cost more than starting on the capable corner:

<ModelEconomy client:visible />

## On a company plan, the ceiling isn't yours

There is a wrinkle that lands hardest for the enterprise reader. When you signed in back in [Getting started](/course/copilot/getting-started/setup/), the setup flagged that at a company providing Copilot, *what you can do is partly decided above your account* - your access and policies come down with your organization's plan. Model availability is one of the specific places that bites.

It connects to the hierarchy you met in the [Rules chapter](/course/copilot/rules/hierarchy/): personal settings, repository settings, and the organization sitting above both. Here that hierarchy shows up as a budget and a menu. Your org's plan and policies can determine [which models appear in your picker](https://docs.github.com/en/copilot/reference/ai-models/supported-models) and how much shared AI-credit budget is available. If a setting you expect is not there, the answer may be a plan or organization policy rather than a VS Code setting. Tune the dials you have, and check [the plans documentation](https://github.com/features/copilot/plans) before treating a number as stable.

## Both dials, one habit

Step back and look at what the last two chapters gave you. Permissions tuned how much Copilot may *do*; models and credits tune what it can figure out and how hard it thinks. Two different dials, one identical instinct: **spend the expensive setting only where it changes the outcome.** Long leash and deep reasoning for the high-stakes library work; short leash and light reasoning for routine app code. You now have both knobs, and the judgment to set them.

There is also a posture change waiting. The careful, in-editor work is behind you; what remains in the course's `orders-service` scenario is a backlog of routine follow-up issues - exactly the low-stakes, well-scoped work you would never spend premium reasoning on. So the next chapter stops doing that work in the editor at all: it hands each issue to a Copilot that works on its own, in the cloud, and comes back with pull requests for your review. [Next chapter: Automation - the cloud coding agent](/course/copilot/automation/).
