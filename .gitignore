# build output
dist/
# generated types
.astro/
.antigravitycli/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

.github/
.sisyphus/

# local Claude Code session state (never commit)
.claude/
.agents
.opencode
.qoder
.idea
.gemini

# unshipped draft chapters (kept outside the docs collection)
src/content/_drafts/
# Strategy/analysis artifacts — must never enter the public repo (live in parent private git)
research/
# Burn/deep-work artifacts dropped at repo root — staging notes, never published
/agentscli-*burn*.md
# impeccable design-context: PRODUCT.md carries strategy/roadmap framing, keep it local.
# (DESIGN.md is pure visual-system capture and is safe to track.)
/PRODUCT.md
/.impeccable/
