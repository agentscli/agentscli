---
title: Cheatsheets
description: Verified command references for the coding agents - every flag read from the installed binary or the official docs, each page with a drill or sandbox to build muscle memory.
---

Every cheatsheet in this section is verified the same way: CLI flags are read first-hand from the installed binary (`--help`), slash commands and subcommands are checked against the tool's official docs, and each page carries the version and date it was checked against. Community cheat sheets are not a source - several popular ones list flags that do not exist.

Each page has two surfaces sharing one dataset:

- **A static reference** - lookup-first tables you can search (press `/` anywhere on a page to jump to the search box) with no JavaScript required.
- **An interactive layer on every page** - Claude Code has a simulated terminal for practicing its session commands (`/compact`, `/rewind`, mode cycling); every other page has a command drill that asks you to type the verified command for a task, with the full answer key readable without JavaScript.

## Pages

- **[Claude Code cheatsheet](/foundations/cheatsheets/claude-code/)** - verified against the installed binary v2.1.234 and the official docs on 2026-08-18.
- **[Codex cheatsheet](/foundations/cheatsheets/codex/)** - CLI reference for the OpenAI coding agent.
- **[OpenCode cheatsheet](/foundations/cheatsheets/opencode/)** - terminal-first reference for the open-source agent.
- **[Cursor cheatsheet](/foundations/cheatsheets/cursor/)** - editor and `cursor-agent` reference.
- **[GitHub Copilot cheatsheet](/foundations/cheatsheets/copilot/)** - CLI, GitHub, and IDE surfaces.
- **[Pi cheatsheet](/foundations/cheatsheets/pi/)** - reference for the extensible terminal agent.
