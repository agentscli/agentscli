---
title: Cursor cheatsheet
description: A verified Cursor reference that separates editor workflows from the cursor-agent terminal CLI, with rules, permissions, MCP, cloud agents, Bugbot, and shortcuts.
---

import DesignContract from "../../../../components/DesignContract.astro";
import CheatDrill from "../../../../components/CheatDrill.astro";
import Cheatsheet from "../../../../components/Cheatsheet.astro";
import UpstreamRefs from "../../../../components/UpstreamRefs.astro";

<DesignContract text={`
AGENTSCLI-CONTRACT · cheatsheets/cursor · 2026-08-18
THESIS: a Cursor cheatsheet should keep the editor surface and the cursor-agent CLI visibly separate before either reference grows long.
OWN-WORLD: the same Starlight lookup surface as the neighboring agent cheatsheets, with evidence labels separating installed-binary facts from official-docs facts.
STORY: meet the two surfaces, learn the CLI session loop, then rules, MCP, cloud agents, and editor shortcuts as the workflow demands.
FIRST VIEWPORT: one framing paragraph, a typing drill over the searchable reference; no simulated terminal is claimed when this page has no runtime sandbox.
FINISH: checked version/date and official upstream references remain visible on the page.
`} />

Cursor has two surfaces that look similar but are not interchangeable: the Cursor editor and the `agent`/`cursor-agent` terminal CLI. The reference below keeps their commands separate. CLI rows were checked against the installed binary `v2026.08.11-e8db854`; editor workflows and documented CLI behavior were checked against Cursor’s official docs on 2026-08-18.

Use the search box for a flag, file, shortcut, or workflow such as `resume`, `permissions`, `mcp`, `Bugbot`, or `model`.

<CheatDrill tool="cursor" />

<Cheatsheet tool="cursor" />

<UpstreamRefs
  checkedAt="2026-08-18"
  links={[
    {
      tool: "Cursor",
      url: "https://github.com/cursor/cursor",
      label: "GitHub repository",
    },
    {
      tool: "Cursor CLI",
      url: "https://cursor.com/docs/cli/overview",
      label: "CLI overview",
    },
    {
      tool: "Cursor CLI",
      url: "https://cursor.com/docs/cli/reference/parameters",
      label: "CLI parameters",
    },
    {
      tool: "Cursor CLI",
      url: "https://cursor.com/docs/cli/reference/permissions",
      label: "CLI permissions",
    },
    {
      tool: "Cursor",
      url: "https://cursor.com/docs/rules",
      label: "rules",
    },
    {
      tool: "Cursor",
      url: "https://cursor.com/docs/advanced/keyboard-shortcuts",
      label: "keyboard shortcuts",
    },
    { tool: "Cursor", url: "https://cursor.com/docs/bugbot", label: "Bugbot" },
  ]}
/>
