---
title: "Custom slash commands"
description: "Save the prompt you keep retyping as a Markdown file in .cursor/commands and fire it with /name - plus the argument-passing limit that decides their shape."
---

## Structure: custom slash commands

A skill is a *procedure the agent reasons through*. Sometimes what you actually keep retyping is simpler: the same **prompt**. "Write a conventional-commit message for the staged diff." "Review this change against `budgetcli`'s money-handling rules" (`budgetcli` is the course's illustrative personal-finance API). Those aren't multi-step procedures the agent should adapt - they're a fixed instruction you want to fire verbatim. That's a **custom slash command**, introduced in **Cursor 1.6** ([changelog](https://cursor.com/changelog/1-6)).

Commands are stored in **`.cursor/commands/`** as plain Markdown files - one file per command, checked into the repo so the team shares them. The filename is the command name; the body is the prompt:

```markdown title=".cursor/commands/commit-msg.md"
Write a conventional-commits message for the currently staged diff.

- Use the budgetcli scopes: importer, ledger, reconcile, api.
- Subject line under 60 characters, imperative mood.
- Body only if the change isn't self-explanatory.
- Never invent a scope that isn't in that list.
```

Now typing `/commit-msg` in Agent chat fires that prompt. It shows up in the `/` picker alongside your skills and subagents - because in Cursor's chat, `/` is the *run* surface and that picker lists everything runnable. One documentation note (as of September 2026): commands are the one extension component without a dedicated docs page of their own - the [Customize overview](https://cursor.com/docs/customize-cursor) describes them as markdown files you invoke with `/` in Agent chat, and the `.cursor/commands/` detail above is what shipped with 1.6 - so this surface is exactly the kind that rots in older tutorials.

### The argument-passing caveat

Here's the edge to know before you build a command expecting it to behave like Claude Code's `$ARGUMENTS`: as of September 2026, text you type after a custom command's name is not passed into the command's prompt - there is no argument placeholder to receive it - and richer templating (argument support, bash execution, file tagging to match Claude Code) is an open community request rather than a shipped feature ([community request thread](https://forum.cursor.com/t/custom-slash-commands-improvements/132611)). The command fires with its fixed prompt; anything variable belongs in the conversation around it.

The practical consequence: write commands that work on the *current context* - the staged diff, the open file, the selection - rather than commands that expect you to pass typed parameters. A `/commit-msg` that reads the staged diff is robust; a `/rename-symbol <old> <new>` that depends on parsing two arguments is fighting the primitive. When you need parameters and branching logic, that's a sign you wanted a **skill** (a procedure) or a **subagent** (a scoped delegated agent you hand a job to), not a command (a template).

### Team Commands

For teams, commands don't have to live in each repo. **Team Commands** (shipped in Cursor 2.0 alongside Team Rules - [changelog](https://cursor.com/changelog/2-0)) let you define custom commands in the Cursor dashboard and have them automatically applied to all members of your team - the same dashboard-distribution model as Team Rules, with no local file needed. A standard like `/commit-msg` then applies to every member without anyone committing a `.cursor/commands/` file.

### A note for anyone who used Notepads

If you used Cursor before late 2025, you reached for **Notepads** to stash reusable prompts and context. Notepads were **deprecated at the end of October 2025** ([announcement](https://forum.cursor.com/t/deprecating-notepads-in-cursor/138305)). Cursor's own post points to **Rules, Commands, Memories, and improved Agent context-discovery** as the features that supersede them (Skills hadn't shipped yet - they arrived later, in [2.4](https://cursor.com/changelog/2-4)). If you're carrying old Notepads, the practical migration this chapter would suggest is to split each one by whether it's a multi-step recipe - now best expressed as a **Skill** - or a fixed instruction, which becomes a **Custom Command**.

Everything this chapter adds still depends, in the end, on the model choosing well. For the one move in `budgetcli` where that is not an assurance you can accept - never letting the agent write to the real ledger - you need something that runs whether or not the model cooperates. [Next: hooks, the gate that can say no](/course/cursor/extending/hooks/).
